\* Wider scope: 3 concurrent processes (3-way interleavings of the lock manager,
\* commits, and quota). Expected: No error (linearizable + deadlock-free at 3 procs).
SPECIFICATION Spec
CONSTANTS
  KEYS = {0, 1}
  CLIENTS = {0}
  PROPS = {0, 1}
  Procs = {1, 2, 3}
  STRIPES = 2
  NoProc = NoProc
  SORTED = TRUE
  MAX_OPS = 2
  QUOTA_LIMIT = 1
PROPERTY Refinement
INVARIANT LockInv
INVARIANT NoLeak
INVARIANT UsedConsistent
