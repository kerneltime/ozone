\* NEGATIVE TEST: argument-order lock acquisition (SORTED = FALSE).
\* Expected result: TLC reports a DEADLOCK (two crossing renames each hold one
\* stripe and wait for the other). This proves the model can express the bug,
\* so the green run of ObsImpl.cfg is meaningful, not vacuous.
SPECIFICATION Spec
CONSTANTS
  KEYS = {0, 1}
  CLIENTS = {0}
  PROPS = {0, 1}
  Procs = {1, 2}
  STRIPES = 2
  NoProc = NoProc
  SORTED = FALSE
  MAX_OPS = 2
  QUOTA_LIMIT = 1
PROPERTY Refinement
INVARIANT LockInv
INVARIANT NoLeak
