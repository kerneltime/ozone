\* Tight bound for a fast, definitive M2a verdict: 2 procs x 1 op on the seeded tree.
\* Pairwise races (create-under-dir vs delete-dir, delete-file vs delete-dir,
\* rename vs delete) all surface at this scale (small-scope hypothesis).
SPECIFICATION Spec
CONSTANTS
  NAMES = {0, 1}
  CLIENTS = {0}
  MAXOID = 3
  Procs = {1, 2}
  NoProc = NoProc
  MAX_OPS = 1
PROPERTY Refinement
INVARIANT LockInv
INVARIANT NoLeak
INVARIANT Accounted
